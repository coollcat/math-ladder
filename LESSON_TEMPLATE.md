# LESSON_TEMPLATE.md · 写课模板（单一事实来源）

> 本文件是生成新课程的**唯一权威模板**。当前已完成的课程是它的参考实现：
> - 黄金样例：`docs/01-arithmetic/10-addition.md`（九段式 + viz + exercise + quiz 全组件）
> - 滑块实验样例：`docs/01-arithmetic/20-subtract-negative.md`
>
> ROADMAP.md 只记进度与选型；本文件管"怎么写"。两者冲突时以本文件为准。

## 0. 三条总原则

1. **一课一概念，长度不设限**：概念边界仍是拆课依据，但不再限制行数/时长——把该讲的讲到透：尽可能 viz 可视化、可交互实验、Python 辅助理解三件套配齐。
   - **难节点大胆详细**：高等数学各卷（卷二起）的章节，以及卷一的硬节点（基与坐标变换、投影、欧拉公式、链式法则、泰勒级数、傅里叶系数等），允许并鼓励大幅展开——多步例题、多层直觉递进、更多实验变体、选读证明写全。「一课一概念」边界不放宽：详细是把一个概念挖深，不是把两个概念挤进一篇。
2. **可视化优先级**：`viz 网页组件`（零等待，人人可玩）→ `浮窗 Python`（改代码级交互、判题）→ `静态文字/公式`（兜底）。能用上一层的就不要用下一层。
3. **首现必注释**：Python 代码里任何**第一次出现**的语法、函数、参数，必须有中文注释说明它是什么。校验器管不到注释，这是写作纪律。
4. **专属组件优先**：交互组件按"本概念最理想的形态"定制，而不是硬套通用类型。写课前先回答："这个概念的完美教具长什么样？"——没有现成组件就在 `src/pyrunner/viz.js` 新增一个以概念命名的渲染器（如 distributive/balance 都是为此而生）。多子代理批量生产时，各章的定制组件清单先汇总、实现后再开写正文。
5. **回填制度**：新组件上线后，回头扫描已有章节找更适配的回填点（ROADMAP 各章下标注回填项），让旧内容持续受益于新工具。

## 1. 文件命名与 front matter

- 目录 `NN-slug/`，文件 `MM-slug.md`，编号步长 10；中间插课用 15、25 等空隙号。
- 单文件 ≤300 行。

```yaml
---
title: 课程标题
lesson_id: chapter-slug/lesson-slug   # 不要用 id 字段（Docusaurus 禁斜杠）
prereqs:                              # 必须指向排在本课之前的课
  - arithmetic/addition
introduces_math: []                   # 本课"发明"的 math.xxx
introduces_builtin: []                # 受管内置函数 abs/sum/min/max/round/pow/divmod
introduces_import: []                 # 第三方库根名，如 [random]
---
```

卷二起的正式课追加全域元数据合同：

```yaml
volume: 2                             # 1–5
layer: L8                             # L0–L11
track:
  - analysis-change                   # 只用 VISION 的八条支线 id
stage: university-core                # primary-intuition / secondary-tool / university-core / research-elective
difficulty: 3                         # 1–5 整数
introduces_concepts: []               # 概念级出生证明
applications: []
exits: []
```

数组一律用 bullet 或空 inline array；没有新工具就留空数组，不为好看塞名词。

## 2. 九段式骨架

| # | 段落 | 要点 | 参考实现 |
| --- | --- | --- | --- |
| 1 | `## 1. 从一个场景开始` | 一个真实生活问题，三句话内轻轻立起悬念 | 加法课 §1 |
| 2 | `## 2. 直觉解释` | 类比 + 图示先行，不放公式 | 减法课 §2 镜像数轴 |
| 3 | `## 3. 正式定义` | KaTeX 公式 + **符号逐一列表说明** | 加法课 §3 表格 |
| 4 | `## 4. 分步例题` | 编号步骤，每步只做一件事 | 加法课 §4 |
| 5 | `## 5. 动手实验` | viz 组件在前，python 块在后（见 §3/§4） | 两节算术课 §5 |
| 6 | 误区卡片 | `:::warning[常见误区]`，2~3 条，写"你以为…其实…" | 各课均有 |
| 7 | 练习 | 判题式 `exercise` 优先；概念题用 `<details>` 折叠详解 | 加法课练习 2 vs 练习 1 |
| 8 | 选读证明 | `<details>` 标注「选读」，跳过不影响主线 | 加法课 §7 |
| 9 | `## 8. 下一站` | 一句话悬念 + 相对链接（带 `.md` 后缀） | 各课末尾 |

## 3. viz 网页可视化组件（优先级最高）

围栏语言 `viz`，内容为 JSON。渲染失败会显示错误原因。

### numberline 数轴跳跃 —— 用于加减法、负数、估算
```viz
{
  "type": "numberline",
  "title": "a - b 的落点",
  "min": -10, "max": 10,
  "op": "-",
  "sliders": [
    { "name": "a", "min": -9, "max": 9, "step": 1, "value": 3 },
    { "name": "b", "min": -9, "max": 9, "step": 1, "value": 8 }
  ]
}
```
粉区=负数、绿区=正数自动绘制；`op` 支持 `"+"` / `"-"`。

### plot 函数图像 —— 用于一次/二次函数、三角、增长对比
```viz
{
  "type": "plot",
  "title": "y = a*x^2 + b",
  "expr": "a*x^2 + b",          // 变量 x + 滑块名；支持 + - * / % ^
  "xmin": -5, "xmax": 5,
  "sliders": [
    { "name": "a", "min": -2, "max": 2, "step": 0.1, "value": 1 },
    { "name": "b", "min": -3, "max": 3, "step": 0.5, "value": 0 }
  ]
}
```

### sines 正弦叠加 —— 波、拍频、傅里叶预告
```viz
{ "type": "sines", "title": "方波的诞生", "terms": [1, 3, 5] }
```

选型口诀：**一个数变 → numberline；两个量关系 → plot；周期运动 → sines**。都不合适才上 python 块画 matplotlib。

### 动手组件（回形针式，直接拖拽/操作，优先于 plot）

| type | 玩法 | 典型章节 |
| --- | --- | --- |
| distributive | 紫点横向切分、橙点纵向拖高度（按钮可精调/重置），实时看 ab+ac | 乘法分配律 |
| divisionshare | 发糖动画滑块，商与余数 | 除法与余数 |
| fraction | 拖圆环改分数，自动显示约分/小数/百分数 | 分数、小数、百分比 |
| fit | 拖数据点，最小二乘直线追着跑 + 误差表 | 一次函数、统计 |
| balance | 「两边同加减」按钮解方程天平 | 一元一次方程 |
| factoring | 正负面积拼块反过来展开 | 因式分解 |
| datachart | 分类频数、条形/直方图，支持零基线讨论 | 数据统计 |
| counting | n/k 联动网格与杨辉点阵 | 排列组合 |

```viz
{ "type": "distributive", "a": 7, "b": 10, "c": 3 }
{ "type": "divisionshare", "total": 23, "people": 4 }
{ "type": "fraction", "value": [3, 4] }
{ "type": "fit", "n": 6 }
{ "type": "balance", "xs": 3, "num": 3 }
```

选型口诀补充：**讲运算律 → distributive；讲除不尽 → divisionshare；讲分数百分数 → fraction；讲拟合/回归 → fit；讲解方程 → balance**。

## 4. Python 块（浮窗运行）

````md
```python title="标题（会显示在浮窗来源里）"
import random  # 首次出现的 import 必须有注释

# sliders: a=3 [-9:9:1], b=8 [-9:9:1]   ← 可选：滑块注入变量 a/b，代码里直接用
fig, ax = plt.subplots(figsize=(7, 2))   # 首现 API 全部尾注或行前注释
```
````

规则：
- `sliders:` 行的变量由系统注入，**代码里不要再赋值**
- 首现语法注释示例：`total = total + n  # 累加器：旧值加新数存回去`
- `input()` 禁用；避免 `while True`

## 5. 判题式练习

````md
```exercise
# @title: 练习标题
# @check: 期望输出第一行
# @check: 第二行（多行逐行比对，空行折叠）
# @hint: 卡住时的提示
初始代码——设计成能跑但结果不对，学生改到通过为止
```
````

初始代码三原则：能直接运行（别只给注释）、结果明显不对、改动点聚焦本课概念。

## 6. 选择题

````md
```quiz
问题文本（纯文字，不要放 KaTeX 公式）？
- 错误项
- 正确项 [*]
? 解释（答对后展示）
```
````

## 7. 折叠块

- 练习详解：`<details><summary>点开查看逐步解答</summary>` 内放分步 markdown；系统会在折叠块前自动注入「▶ 用 Python 解题」浮窗入口，草稿保存在学生本机
- 选读证明：summary 以「选读」开头

## 8. 发布自检清单

- [ ] 一课一概念？（难节点课可大胆详细；若超长，先自查是否混入了第二个概念）
- [ ] 每个实验都有 viz 或滑块版？纯文字实验不合格
- [ ] Python 首现语法全部有中文注释？
- [ ] prereqs 指向存在且更前的课？front matter 登记了所有新工具？
- [ ] `npm run validate` 绿 → `npm run build` 绿 → 勾 ROADMAP checkbox

## 9. 统一术语表

| 术语 | 固定用法 |
| --- | --- |
| 命题 | 可判定真假的陈述句；不用“陈述句”泛指 |
| 谓词 | 带自由变量的命题函数；量词绑定后才有真假 |
| 论域 | 量词的讨论范围；每次使用量词必须明确 |
| 充分条件 | 有它足够推出结论，不表示必要 |
| 必要条件 | 结论成立离不开它，不表示单独充分 |
| 反例 | 满足前提但否定结论的具体对象 |
| 反证 | 假设否定结论并推出矛盾 |
| 归纳法 | 基础步 + 归纳步；不等于“举例归纳” |
| 邻域 | 以点为中心的开放范围；避免只说“附近” |
| 上确界 | 最小上界；不等于最大值 |
| 开集 | 每点都有邻域留在集合内 |
| 收敛 | 极限存在且唯一；数列与函数分开表述 |
| 一致收敛 | 收敛速度对定义域一致；不与逐点收敛混用 |
| 线性空间 | 满足八条运算律的向量集合 |
| 秩 | 极大线性无关组所含向量个数 |
| 特征向量 | 变换后方向不变的非零向量 |
| Jacobian | 向量函数一阶偏导矩阵 |
| Hessian | 二阶偏导对称矩阵 |
| 条件概率 | 缩小论域后的概率，不是因果强度 |
| 期望 | 概率加权平均；不保证典型取值 |
| 熵 | 分布不确定性的期望自信息 |
| KL 散度 | 非对称分布差异；不是距离 |
| 泛化 | 训练分布之外的预测能力 |
| 过拟合 | 训练误差低但验证/泛化误差高 |
| 因果效应 | 干预引起的分布变化；不等同相关 |
